{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "dist/api/**/*.js": {
      "runtime": "edge"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}