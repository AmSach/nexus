{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "dist/api/**/*.js": {
      "maxDuration": 60,
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
